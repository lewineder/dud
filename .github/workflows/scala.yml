name: Scala CI

on:
  push:
    branches: [ main, dev, aufgabe6 ]
  pull_request:
    branches: [ main, dev, aufgabe6 ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
      - name: Run tests
        run: sbt test
